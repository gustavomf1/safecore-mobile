class AppConfig {
  static const apiBaseUrl = 'https://api.safecoreteste.online';
  static const bffBaseUrl = 'https://mobile-api.safecoreteste.online';
  static const connectTimeout = Duration(seconds: 30);
  static const receiveTimeout = Duration(seconds: 60);
}
